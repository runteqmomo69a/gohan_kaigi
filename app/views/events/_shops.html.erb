<div class="mt-8">
  <div class="card bg-base-100 shadow-md rounded-xl">
    <div class="card-body">
      <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
        <h2 class="text-xl font-bold">お店候補</h2>

        <div class="flex flex-wrap items-center gap-2">
          <%= link_to "登録順", event_path(event, sort: "created_at"), class: "btn btn-outline btn-sm" %>
          <%= link_to "いいね順", event_path(event, sort: "likes_count"), class: "btn btn-outline btn-sm" %>
          <%= link_to "お店候補を登録", new_event_shop_path(event), class: "btn btn-primary btn-sm" %>
        </div>
      </div>

      <% if shops.any? %>
        <div class="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
          <% shops.each do |shop| %>
            <div class="rounded-xl border border-base-300 bg-base-100 p-4 shadow-sm transition hover:shadow-md">
              <div class="flex items-start justify-between gap-3">
                <p class="text-lg font-semibold leading-snug"><%= shop.name %></p>
                <span class="shrink-0 text-sm font-medium text-gray-500">❤️ <%= shop.likes_count %></span>
              </div>

              <% if shop.url.present? %>
                <p class="mt-2 text-sm">
                  <a href="<%= shop.url %>" target="_blank" class="text-blue-500 underline">
                    お店のURL
                  </a>
                </p>
              <% end %>

              <% if shop.memo.present? %>
                <p class="mt-2 text-sm text-gray-600 line-clamp-3"><%= shop.memo %></p>
              <% end %>

              <p class="mt-3 text-xs text-gray-400">
                登録者：<%= shop.user.name %>
              </p>

              <div class="mt-4">
                <% if current_user.likes.exists?(shop: shop) %>
                  <%= link_to "いいね解除",
                    event_shop_like_path(event, shop),
                    data: { turbo_method: :delete },
                    class: "btn btn-outline btn-sm w-full" %>
                <% else %>
                  <%= link_to "いいね",
                    event_shop_like_path(event, shop),
                    data: { turbo_method: :post },
                    class: "btn btn-primary btn-sm w-full" %>
                <% end %>
              </div>

              <% if shop.user == current_user %>
                <div class="mt-3 flex gap-2">
                  <%= link_to "編集",
                    edit_event_shop_path(event, shop),
                    class: "btn btn-outline btn-sm flex-1" %>

                  <%= link_to "削除",
                    event_shop_path(event, shop),
                    data: { turbo_method: :delete, turbo_confirm: "このお店候補を削除します。よろしいですか？" },
                    class: "btn btn-error btn-sm flex-1" %>
                </div>
              <% end %>

              <details class="mt-3">
                <summary class="btn btn-outline btn-sm w-full">地図を見る</summary>

                <div class="mt-3 overflow-hidden rounded-lg border border-base-300">
                  <iframe
                    src="<%= shop.map_embed_url(event.place) %>"
                    width="100%"
                    height="220"
                    style="border:0;"
                    loading="lazy"
                    allowfullscreen>
                  </iframe>
                </div>

                <div class="mt-2 text-right">
                  <%= link_to "Google Mapで開く",
                    shop.map_link_url(event.place),
                    target: "_blank",
                    rel: "noopener",
                    class: "link link-primary text-sm" %>
                </div>
              </details>
            </div>
          <% end %>
        </div>
      <% else %>
        <p class="mt-2 text-sm text-gray-500">まだお店候補はありません</p>
      <% end %>
    </div>
  </div>
</div>